import { muatations } from "./muatation";
import { resolvers } from "./resolver";
import { types } from "./types";
import {queries} from "./queries"


export const Post={types,muatations,resolvers,queries}

